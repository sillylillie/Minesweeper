import numpy as np

def solution(solver, turn):
	print('solver: {}'.format(solver))
	print('bombs: {}'.format(turn['BOMBS']))
	print('time: {}'.format(turn['TIME']))
	# print('board: \n{}'.format(np.mat(turn['BOARD'])))

	return [],[]